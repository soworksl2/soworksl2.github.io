{
  "name": "portfolio_soworksl",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "devbuild": "cp ./public/ ./dev_dist/ -r && npx pug index.pug -o dev_dist/ -P",
    "build": "cp ./public ./dist/ -r && npx pug index.pug -o dist/ -P",
    "gh-deploy": "npm run build && npx gh-pages -d dist/"
  },
  "keywords": [],
  "author": "soworksl",
  "license": "GPL V3.0",
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "pug-cli": "^1.0.0-alpha6"
  }
}
